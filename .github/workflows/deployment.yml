name: Development - Deploy static website

on:
  push:
    branches: [ "main" ]

env:
  AWS_REGION: ap-south-1
  S3_BUCKET: "demoaryanayush"

permissions:
  contents: read

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    environment: development

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.AKIAVDHC7ADHNWLFOR5G }}
        aws-secret-access-key: ${{ secrets.KP2lgVqKlyvyV/SSJ+J+25r+Ll/57jANrqBoTYOC}}
        aws-region: ${{ env.AWS_REGION }}
        
    - name: Copy files
      run: |
          aws s3 cp . s3://${{ env.S3_BUCKET }}/ --recursive --exclude "*" --include "*.html" --include "images/*"
          echo "Deployment done"
